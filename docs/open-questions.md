# Open Questions

1. How is `assemblyId` mapped from a Revit element? Is it a Revit parameter or derived from `UniqueId`?
2. Which Revit parameter name contains the Stratus object type (Assembly vs Package vs Part)?
3. Is there a bulk tracking status endpoint, or must we call one at a time?
4. Are there rate limits on the Stratus API?
5. What is the shape of the `divisionId` field — is it a GUID?
